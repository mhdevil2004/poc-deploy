DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_NAME=loan_api
DB_PASSWORD=replace-with-your-password
PORT=8080
JWT_SECRET=Fintillaa-local-dev-secret-change-before-production-2026
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:5500,null

# ── AI Agent (Gemini) ─────────────────────────────────────────────────────
# Obtain a free API key at: https://aistudio.google.com/app/apikey
# NEVER commit this value to version control.
GEMINI_API_KEY=your-gemini-api-key-here
# Optional: override the model. Default is gemini-3.5-flash-lite (free tier).
GEMINI_MODEL=gemini-3.5-flash-lite

