# Frontend

The customer and lender portal, built with Next.js.

Copy `.env.example` to `.env.local`, set `NEXT_PUBLIC_API_URL`, then run:

```bash
npm ci
npm run dev
```

The app is served at `http://localhost:3000`.
